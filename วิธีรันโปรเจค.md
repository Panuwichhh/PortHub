# วิธีรันโปรเจค PortHub

รัน **3 อย่าง** ตามลำดับ: Database → Backend → Frontend

---

## สิ่งที่ต้องมี

- **PostgreSQL** รันอยู่ (localhost:5432)
- **Go** (สำหรับ backend)
- **Node.js** (สำหรับ frontend)

---

## ขั้นที่ 1: Database (PostgreSQL)

ให้แน่ใจว่า PostgreSQL รันอยู่ และมี database ชื่อ `porthub_db`  
(ถ้าใช้ Docker: `cd backend` แล้ว `docker-compose up -d postgres`)

**ถ้าเป็น DB ใหม่:** รัน script สร้างตาราง  
`backend/database/init.sql`

**ถ้าเป็น DB เดิมที่ใช้อยู่แล้ว:** รัน migration เพิ่มคอลัมน์  
`backend/database/add_show_on_dashboard.sql`  
หรือรันคำสั่ง SQL นี้ใน DB:

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS show_on_dashboard BOOLEAN DEFAULT false;
```

Backend โปรเจคนี้ต่อ DB ด้วย: **user=postgres, password=190946, dbname=porthub_db**

---

## ขั้นที่ 2: Backend (Go API)

เปิด **Terminal ที่ 1**:

```bash
cd backend
go run .
```

รอจนเห็นข้อความประมาณ:

```
✅ Database connected successfully
🔥 [SERVER START] http://localhost:8080
```

แปลว่า API พร้อมใช้ที่ **http://localhost:8080** (อย่าปิด terminal นี้)

---

## ขั้นที่ 3: Frontend (Next.js)

เปิด **Terminal ที่ 2** (terminal ใหม่):

```bash
cd frontend
npm install
npm run dev
```

รอจนเห็นข้อความประมาณ:

```
▲ Next.js ...
- Local: http://localhost:3000
✓ Ready
```

แปลว่าเว็บรันที่ **http://localhost:3000**

---

## สรุป

| สิ่งที่รัน        | พอร์ต | URL                    |
|------------------|-------|------------------------|
| PostgreSQL       | 5432  | (ใช้ภายใน backend)     |
| Backend (Go)     | 8080  | http://localhost:8080  |
| Frontend (Next)  | 3000  | http://localhost:3000  |

**เปิดเบราว์เซอร์ไปที่:** http://localhost:3000

---

## ถ้าเจอปัญหา

- **Database connect ไม่ได้** → ตรวจว่า PostgreSQL รันอยู่ และ user/password/dbname ตรงกับใน `backend/main.go` (ตอนนี้เป็น postgres / 190946 / porthub_db)
- **column "show_on_dashboard" does not exist** → ต้องรัน migration ตามขั้นที่ 1
- **Frontend เรียก API ไม่ได้** → ตรวจว่า Backend รันอยู่ที่พอร์ต 8080 และไม่มี firewall บล็อก
